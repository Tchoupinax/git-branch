name: Pull request checks 🔎

on:
  pull_request:

jobs:
  lint:
    name: Check code quality 🫆
    runs-on: ubuntu-latest
    steps:
      - name: Check code quality 🫆
        uses: tchoupinax/repo-config/shared-actions/steps/golang/golangci@v1.0.0

  build:
    name: Check build success 🦫
    runs-on: ubuntu-latest
    steps:
      - name: Check build success 🦫
        uses: tchoupinax/repo-config/shared-actions/steps/golang/goreleaser-build-only@v1.0.0

  labels:
    name: Autodetect labels 🔍 
    runs-on: ubuntu-latest
    steps:
      - name: Autodetect labels 🔍 
        uses: tchoupinax/repo-config/shared-actions/dx/automatically-apply-labels@v1.0.0
        with:
          appId: ${{ secrets.DEPLOY_APP_ID }}
          privateKey: ${{ secrets.DEPLOY_APP_KEY }}
