name: Release

on:
  push:
    tags:
      - "*"

jobs:
  release:
    name: Release project 🚀
    runs-on: ubuntu-latest
    steps:
      - name: Release project 🚀
        uses: tchoupinax/repo-config/shared-actions/golang/goreleaser-release@3df5047cbd8923c8083df349431385edfdda2d13
        with:
          appId: ${{ secrets.DEPLOY_APP_ID }}
          privateKey: ${{ secrets.DEPLOY_APP_KEY }}

  brew-update:
    name: Update Brew registry 👊
    runs-on: ubuntu-latest
    needs: [release]
    steps:
      - name: Update Brew registry 👊
        uses: tchoupinax/repo-config/shared-actions/dx/update-brew-registry@b6112ca3fb39559ee7408dde1e22a0c5f94e0fbe
        with:
          patToken: ${{ secrets.BREW_COMITTER_PAT }}
          formulaPath: ssh-list.rb
